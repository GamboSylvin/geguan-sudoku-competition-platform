# Samples — put the sample files here

> **[[FILL-BEFORE-UNIT: import units — the two sample files below = ________ ; owner: the person completing the context]]**

The person completing the context provides two sample files and places them **in this folder**, with exactly these names:

| File to place here | What it is | Needed for |
|---|---|---|
| `participants-sample.xlsx` | A sample participant Excel (columns Name, School, Category, Team, and any extra columns) | The participant import unit; the participant numbering and credentials |
| `question-sample.pdf` | A sample question PDF in the strict predefined format | The question import unit; the grid shapes; the answer check; whether the PDF carries the points |

After placing them, fill the related blanks (see `../FILL-BEFORE-CODING.md`, section B):
- the extra Excel columns (U-40)
- whether the question PDF carries the points (U-03)
- the grid shapes in the PDF (U-01)
- whether every puzzle has a unique solution (U-90)

**Privacy:** if the sample Excel contains real student names, replace them with fake ones before placing the file here. This folder is part of the context and may be read by an agent.
